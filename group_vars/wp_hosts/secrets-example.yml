---

# Secrets applying only to wordpress hosts

# Copy this example file to secrets.yml in this folder then
# set and uncoment the variables below

# default_restic_backup_url: "restic-repo-backup-url-here"
# default_restic_backup_repo_password: "restic-repo-pasword-here"
# default_object_store_access_key_id: "object-store-api-key-id"
# default_object_store_access_key: "object-store-api-key"
# default_admin_user: "default-wordpress-admin-username" should not contain special characters like quotes, brackets etc.
# default_admin_password: "default-initial-wordpress-admin-password" should not contain special characters like quotes, brackets etc.
# default_db_user: "wordpress-db-username" should not contain special characters like quotes, brackets etc.
# default_db_user_password: "db-user-password" should not contain special characters like quotes, brackets etc.
# default_db_root_password: "db-root-password" should not contain special characters like quotes, brackets etc.
# default_db_name: "wordpress-db-name" should not contain special characters like quotes, brackets etc.
